/* Kishore Studios public configuration.
   These are client-side values, so NEVER put Supabase service_role keys or Cloudinary API secrets here. */
window.KS_CONFIG = {
  supabaseUrl: 'https://wyqulehgazdabgxfwkmp.supabase.co',
  // Get this from Supabase Dashboard → Settings → API Keys → Publishable key (or legacy anon key).
  supabaseAnonKey: 'PASTE_SUPABASE_PUBLISHABLE_KEY_HERE',
  // Get this from Cloudinary Console → Product Environment → API Keys.
  cloudinaryCloudName: 'PASTE_CLOUDINARY_CLOUD_NAME_HERE',
  cloudinaryUploadPreset: 'kishore_gallery'
};
